// #pragma GCC optimize("Ofast,unroll-loops")
// #pragma GCC target("avx2")

#include <bits/stdc++.h>

bool multitest = 0;

using namespace std;
using uint = uint32_t;
using i64 = int64_t;

void solve()
{
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int t = 1;
    if (multitest)
        cin >> t;
    while (t-- > 0)
        solve();
}